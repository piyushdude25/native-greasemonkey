import React from "react";
import { View, Text } from "react-native-web";

const CartStore = () => {
  return (
    <View>
      <Text>CartStore</Text>
    </View>
  );
};

export default CartStore;
